import { NOIR, CREME, OR } from '@/lib/utils'
export default function Tarifs() {
  return <div style={{ padding: '32px', background: CREME, minHeight: '100vh', color: NOIR }}><h1 style={{ color: OR }}>Tarifs — Module 3</h1></div>
}
